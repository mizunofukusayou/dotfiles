_: {
  services.skhd = {
    enable = true;
    config = ''
      ctrl + alt - b : open -a "Zen.app"
      ctrl + alt - t : open -a "WezTerm.app"
      ctrl + alt - m : open -a "Amazon Music.app"
    '';
  };
}
